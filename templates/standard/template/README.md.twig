# {{ nodes['name'].text }}

## Requirements

## Installation

Open terminal and execute next line:

```shell
{% if nodes['type.cli'] %}
composer global require {{ nodes['name'].text }}
{{ nodes['type.cli.namespace'].text }}
{% else %}
composer install
{% endif %}
```

{% if nodes['type.cli'] %}
## Quick Start

* Open terminal and execute next line:
```
make build
make run
```
{% if nodes['type.web.containerization.docker.server'] %}
* Open localhost:8000
{% endif %}

{% endif %}
